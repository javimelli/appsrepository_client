/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DatasetDetalleComponent } from './dataset-detalle.component';

describe('Component: DatasetDetalle', () => {
  it('should create an instance', () => {
    let component = new DatasetDetalleComponent();
    expect(component).toBeTruthy();
  });
});
